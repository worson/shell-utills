# 安装

sudo apt-get install samba
sudo apt-get install smbclient


# 配置
# sudo gedit /etc/samba/smb.conf

# 操作
# 增加用户：sudo useradd smbuser
# 设置用户密码：sudo smbpasswd -a smbuser"
# 重启服务：sudo service smbd restart
